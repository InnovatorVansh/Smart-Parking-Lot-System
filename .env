# Environment Variables
# These variables can be accessed in your Vite app using import.meta.env.VITE_VARIABLE_NAME

# Example backend API URL (uncomment and adjust if you add a backend later)
# VITE_API_URL=https://your-backend-api.onrender.com

# Deployment specific settings (optional for Render)
# Node version to use on Render (Render looks for this in package.json or an env var, though it's usually set in the Render dashboard)
# NODE_VERSION=18.18.0

# When deploying to Render, your build command should be: npm run build
# Your publish directory should be: dist
